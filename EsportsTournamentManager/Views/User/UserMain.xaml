<UserControl x:Class="EsportsTournamentManager.Views.User.UserMain"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:tournaments="clr-namespace:EsportsTournamentManager.Views.User.Tournaments"
             xmlns:dashboard="clr-namespace:EsportsTournamentManager.Views.User.Dashboard"
             xmlns:player="clr-namespace:EsportsTournamentManager.Views.User.Players"
             xmlns:teams="clr-namespace:EsportsTournamentManager.Views.User.Teams"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1100">
    <UserControl.Resources>
        <!-- Hyperlink Button Style -->
        <Style x:Key="HyperlinkButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#6366F1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" TextDecorations="Underline" Foreground="{TemplateBinding Foreground}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#818CF8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Search TextBox Style -->
        <Style x:Key="SearchTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6366F1"/>
                                <Setter TargetName="border" Property="Background" Value="#0F172A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Stats Grid Style (Read Only) -->
        <Style x:Key="StatDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="RowBackground" Value="#0F172A"/>
            <Setter Property="AlternatingRowBackground" Value="#1E293B"/>
            <Setter Property="Foreground" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="RowHeight" Value="32"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#1E293B"/>
                        <Setter Property="Foreground" Value="#9CA3AF"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="6,4"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#334155"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="6,0"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter SnapsToDevicePixels="True"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <!-- Sidebar -->
            <ColumnDefinition Width="*"/>
            <!-- Workspace -->
        </Grid.ColumnDefinitions>

        <!-- Left Sidebar for User -->
        <Border Grid.Column="0" Background="{StaticResource SidebarBg}" CornerRadius="15,0,0,15" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="80"/>
                    <!-- Logo Area -->
                    <RowDefinition Height="*"/>
                    <!-- Navigation Menu -->
                    <RowDefinition Height="Auto"/>
                    <!-- Active User Profile & Logout -->
                </Grid.RowDefinitions>

                <!-- Header Branding -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                    <Border Width="36" Height="36" Background="{StaticResource AccentColor}" CornerRadius="10">
                        <Path Fill="#FFFFFF" Data="M8,6 L28,6 L25,18 C22,25 14,25 11,18 Z" HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="Uniform" Margin="6"/>
                    </Border>
                    <StackPanel Margin="10,0,0,0">
                        <TextBlock Text="ESPORTS" Foreground="{StaticResource TextLight}" FontSize="16" FontWeight="Black"/>
                        <TextBlock Text="MEMBER" Foreground="{StaticResource TextMuted}" FontSize="10" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>

                <!-- Navigation Sidebar Menu -->
                <StackPanel Grid.Row="1" Margin="0,20,0,0">
                    <Button x:Name="BtnMenuHome" Style="{StaticResource ActiveSidebarButton}" Margin="10,4,10,4" Click="BtnMenuHome_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⌂" FontSize="18" Margin="0,0,10,0" FontWeight="Bold"/>
                            <TextBlock Text="Trang chủ" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnMenuTournaments" Style="{StaticResource SidebarButton}" Margin="10,4,10,4" Click="BtnMenuTournaments_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🏆" FontSize="16" Margin="0,0,10,0"/>
                            <TextBlock Text="Giải đấu" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnMenuPlayers" Style="{StaticResource SidebarButton}" Margin="10,4,10,4" Click="BtnMenuPlayers_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👤" FontSize="16" Margin="0,0,10,0"/>
                            <TextBlock Text="Tuyển thủ" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnMenuTeams" Style="{StaticResource SidebarButton}" Margin="10,4,10,4" Click="BtnMenuTeams_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🛡️" FontSize="16" Margin="0,0,10,0"/>
                            <TextBlock Text="Đội tuyển" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Footer Active User profile & Logout -->
                <Border Grid.Row="2" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,1,0,0" Padding="20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock x:Name="TxtUserHeaderUserName" Text="User Name" Foreground="{StaticResource TextLight}" FontSize="14" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                            <TextBlock x:Name="TxtUserHeaderUserRole" Text="Người dùng" Foreground="{StaticResource TextMuted}" FontSize="12"/>
                        </StackPanel>

                        <Button Grid.Column="1" Content="➔" Background="Transparent" BorderBrush="Transparent" Foreground="#EF4444" FontSize="18" FontWeight="Bold" Cursor="Hand" Click="LogoutButton_Click" ToolTip="Đăng xuất">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="Transparent">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#F87171"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!-- Right Content Workspace for User -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <!-- Top Header -->
                <RowDefinition Height="*"/>
                <!-- Central Content Frame -->
            </Grid.RowDefinitions>

            <!-- Top Header Bar -->
            <Border Grid.Row="0" Background="{StaticResource HeaderBg}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1" Padding="30,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Section Title -->
                    <TextBlock Text="Trang thành viên" Foreground="{StaticResource TextLight}" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>

                    <!-- Window Controls -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Maximize Window button -->
                        <Button Content="🗖" Background="Transparent" BorderBrush="Transparent" Foreground="{StaticResource TextMuted}" FontSize="15" Cursor="Hand" Click="MaximizeButton_Click" Margin="0,0,15,0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}"/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#6366F1"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <!-- Close Window button -->
                        <Button Content="✕" Background="Transparent" BorderBrush="Transparent" Foreground="{StaticResource TextMuted}" FontSize="18" Cursor="Hand" Click="CloseButton_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}"/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#EF4444"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Central Workspace Panels -->
            <Grid Grid.Row="1" Margin="30">
                <!-- Panel 1: Home Dashboard -->
                <dashboard:UserDashboardView x:Name="PanelDashboard" Visibility="Visible"/>

                <!-- Panel 2: Tournaments List -->
                <tournaments:UserTournamentsView x:Name="PanelTournaments" Visibility="Collapsed" Margin="0,0,10,0"/>

                <!-- Panel 2.5: Players List -->
                <player:UserPlayersView x:Name="PanelPlayers" Visibility="Collapsed" Margin="0,0,10,0"/>

                <!-- Panel 2.6: Teams List -->
                <teams:UserTeamsView x:Name="PanelTeamsUser" Visibility="Collapsed" Margin="0,0,10,0"/>

                <!-- Panel 2: Tournament Overview -->
                <tournaments:TournamentOverview x:Name="PanelTournamentOverview" Visibility="Collapsed"/>

                <!-- Panel 3: Tournament Bracket -->
                <Grid x:Name="PanelTournamentBracket" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Header (Back + Title) -->
                        <RowDefinition Height="*"/>    <!-- Content Area (Full Bracket Visual) -->
                    </Grid.RowDefinitions>

                    <!-- Back & Title -->
                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="← Quay lại Tổng quan" Style="{StaticResource HyperlinkButton}" Foreground="{StaticResource TextMuted}" FontSize="14" FontWeight="SemiBold" Click="BtnBackToOverview_Click" Margin="0,0,20,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TxtTournamentBracketHeader" Grid.Column="1" Text="NHÁNH ĐẤU &amp; KẾT QUẢ" Foreground="{StaticResource TextLight}" FontSize="18" FontWeight="Black" VerticalAlignment="Center"/>
                    </Grid>

                    <!-- Bracket View Area -->
                    <Border Grid.Row="1" Background="#0F172A" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="8" Padding="15">
                        <Grid>
                            <ScrollViewer x:Name="ScrollBracket" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                                <Grid>
                                    <Canvas x:Name="CanvasBracket" Width="1200" Height="600" Background="Transparent" Visibility="Visible"/>
                                    <ScrollViewer x:Name="ScrollRoundRobin" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                                        <StackPanel x:Name="PanelRoundRobin" Margin="10"/>
                                    </ScrollViewer>
                                </Grid>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Panel 4: Team Detail -->
                <Grid x:Name="PanelTeamDetail" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Back button -->
                        <RowDefinition Height="Auto"/> <!-- Header details -->
                        <RowDefinition Height="*"/>    <!-- Stats split -->
                    </Grid.RowDefinitions>

                    <Button Grid.Row="0" Content="← Quay lại" Style="{StaticResource HyperlinkButton}" Foreground="{StaticResource TextMuted}" FontSize="14" FontWeight="SemiBold" Click="BtnNavigateBack_Click" Margin="0,0,0,15"/>

                    <Border Grid.Row="1" Background="#111827" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="200"/>
                            </Grid.ColumnDefinitions>
                            <Ellipse Grid.Column="0" Width="60" Height="60" Fill="#6366F1" Margin="0,0,20,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock x:Name="TxtTeamNameTitle" Text="Team Name" Foreground="{StaticResource TextLight}" FontSize="22" FontWeight="Black"/>
                                <TextBlock x:Name="TxtTeamCoach" Text="HLV: Unknown" Foreground="{StaticResource TextMuted}" FontSize="13" Margin="0,4,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                <TextBlock Text="TỈ LỆ THẮNG" Foreground="{StaticResource TextMuted}" FontSize="10" FontWeight="Bold"/>
                                <TextBlock x:Name="TxtTeamWinRate" Text="0.0%" Foreground="#10B981" FontSize="26" FontWeight="Black"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="4*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left Panel: Players List -->
                        <Border Grid.Column="0" Background="#0F172A" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,20,0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Text="DANH SÁCH TUYỂN THỦ" Foreground="{StaticResource TextMuted}" FontSize="11" FontWeight="Bold" Margin="0,0,0,10"/>
                                <ListView x:Name="ListTeamPlayers" Grid.Row="1" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextLight}">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="#1E293B" BorderThickness="0,0,0,1" Padding="0,8" Width="220">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Button Grid.Column="0" Content="{Binding InGameName}" Tag="{Binding}" Style="{StaticResource HyperlinkButton}" FontSize="13" FontWeight="Bold" Click="PlayerLink_Click"/>
                                                    <TextBlock Grid.Column="1" Text="{Binding Position}" Foreground="{StaticResource TextMuted}" FontSize="11" VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </Grid>
                        </Border>

                        <!-- Right Panel: Stats & Recent Matches -->
                        <Grid Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Team Average stats -->
                            <Border Grid.Row="0" Background="#0F172A" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                                <StackPanel>
                                    <TextBlock Text="HIỆU SUẤT TRUNG BÌNH" Foreground="{StaticResource TextMuted}" FontSize="11" FontWeight="Bold" Margin="0,0,0,10"/>
                                    <UniformGrid Columns="3">
                                        <StackPanel>
                                            <TextBlock Text="Mạng hạ gục/trận" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                            <TextBlock x:Name="TxtTeamAvgKills" Text="0.0" Foreground="{StaticResource TextLight}" FontSize="18" FontWeight="Bold" Margin="0,4,0,0"/>
                                        </StackPanel>
                                        <StackPanel>
                                            <TextBlock Text="Sát thương/trận" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                            <TextBlock x:Name="TxtTeamAvgDamage" Text="0" Foreground="{StaticResource TextLight}" FontSize="18" FontWeight="Bold" Margin="0,4,0,0"/>
                                        </StackPanel>
                                        <StackPanel>
                                            <TextBlock Text="Chỉ số lính/trận" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                            <TextBlock x:Name="TxtTeamAvgCS" Text="0" Foreground="{StaticResource TextLight}" FontSize="18" FontWeight="Bold" Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </UniformGrid>
                                </StackPanel>
                            </Border>

                            <!-- Recent Matches -->
                            <Border Grid.Row="1" Background="#0F172A" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="8" Padding="15">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Text="TRẬN ĐẤU GẦN ĐÂY" Foreground="{StaticResource TextMuted}" FontSize="11" FontWeight="Bold" Margin="0,0,0,10"/>
                                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                        <ItemsControl x:Name="ListTeamRecentMatches">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,8" Cursor="Hand" MouseDown="MatchRow_MouseDown">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="100"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="100"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Grid.Column="0" Text="{Binding Team1.Acronym}" Foreground="{StaticResource TextLight}" FontSize="12" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                                            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                                                                <TextBlock Text="{Binding Team1Score}" Foreground="{StaticResource TextLight}" FontSize="14" FontWeight="Black"/>
                                                                <TextBlock Text=":" Foreground="{StaticResource TextMuted}" FontSize="14" Margin="5,0"/>
                                                                <TextBlock Text="{Binding Team2Score}" Foreground="{StaticResource TextLight}" FontSize="14" FontWeight="Black"/>
                                                            </StackPanel>
                                                            <TextBlock Grid.Column="2" Text="{Binding Team2.Acronym}" Foreground="{StaticResource TextLight}" FontSize="12" FontWeight="Bold" HorizontalAlignment="Right" TextTrimming="CharacterEllipsis"/>
                                                            <Border Grid.Column="3" Background="#1E1B4B" CornerRadius="4" Padding="5,2" Margin="15,0,0,0">
                                                                <TextBlock Text="{Binding Tournament.GameType}" Foreground="{StaticResource AccentColor}" FontSize="9" FontWeight="Bold"/>
                                                            </Border>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                </Grid>

                <!-- Panel 5: Player Detail -->
                <Grid x:Name="PanelPlayerDetail" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Back button -->
                        <RowDefinition Height="Auto"/> <!-- Header details -->
                        <RowDefinition Height="*"/>    <!-- Stats split -->
                    </Grid.RowDefinitions>

                    <Button Grid.Row="0" Content="← Quay lại" Style="{StaticResource HyperlinkButton}" Foreground="{StaticResource TextMuted}" FontSize="14" FontWeight="SemiBold" Click="BtnNavigateBack_Click" Margin="0,0,0,15"/>

                    <Border Grid.Row="1" Background="#111827" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="200"/>
                            </Grid.ColumnDefinitions>
                             <Border Grid.Column="0" Width="60" Height="60" Background="#1E1B4B" CornerRadius="30" Margin="0,0,20,0" ClipToBounds="True">
                                <Grid>
                                    <TextBlock x:Name="TxtPlayerPlaceholder" Text="👤" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    <Image x:Name="ImgPlayerAvatar" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock x:Name="TxtPlayerInGameName" Text="Faker" Foreground="{StaticResource TextLight}" FontSize="22" FontWeight="Black"/>
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock x:Name="TxtPlayerRealName" Text="Lee Sang-hyeok" Foreground="{StaticResource TextMuted}" FontSize="13"/>
                                    <TextBlock Text=" • " Foreground="{StaticResource TextMuted}" FontSize="13"/>
                                    <TextBlock x:Name="TxtPlayerNationality" Text="Hàn Quốc" Foreground="{StaticResource TextMuted}" FontSize="13"/>
                                    <TextBlock Text=" • " Foreground="{StaticResource TextMuted}" FontSize="13"/>
                                    <TextBlock x:Name="TxtPlayerAge" Text="30 tuổi" Foreground="{StaticResource TextMuted}" FontSize="13"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                <TextBlock Text="ĐỘI TUYỂN" Foreground="{StaticResource TextMuted}" FontSize="10" FontWeight="Bold"/>
                                <Button x:Name="TxtPlayerTeam" Content="T1 Esports" Style="{StaticResource HyperlinkButton}" Foreground="#6366F1" FontSize="16" FontWeight="Black" Click="TeamLink_Click"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4*"/>
                            <ColumnDefinition Width="3*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left Panel: Stats and Champion Pool -->
                        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,20,0">
                            <StackPanel>
                                <!-- Stats UniformGrid -->
                                <Border Background="#0F172A" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                                    <StackPanel>
                                        <TextBlock Text="HIỆU SUẤT TRUNG BÌNH" Foreground="{StaticResource TextMuted}" FontSize="11" FontWeight="Bold" Margin="0,0,0,10"/>
                                        <UniformGrid Columns="2">
                                            <StackPanel Margin="0,0,10,12">
                                                <TextBlock Text="KDA Trung bình:" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                                <TextBlock x:Name="TxtPlayerAvgKDA" Text="0.0 / 0.0 / 0.0" Foreground="{StaticResource TextLight}" FontSize="15" FontWeight="Bold" Margin="0,4,0,0"/>
                                            </StackPanel>
                                            <StackPanel Margin="0,0,0,12">
                                                <TextBlock Text="Điểm PTS Trung bình:" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                                <TextBlock x:Name="TxtPlayerAvgPTS" Text="0.0" Foreground="#F59E0B" FontSize="15" FontWeight="Bold" Margin="0,4,0,0"/>
                                            </StackPanel>
                                            <StackPanel Margin="0,0,10,12">
                                                <TextBlock Text="Sát thương trung bình:" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                                <TextBlock x:Name="TxtPlayerAvgDamage" Text="0" Foreground="{StaticResource TextLight}" FontSize="15" FontWeight="Bold" Margin="0,4,0,0"/>
                                            </StackPanel>
                                            <StackPanel Margin="0,0,0,12">
                                                <TextBlock Text="Chỉ số lính trung bình:" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                                <TextBlock x:Name="TxtPlayerAvgCS" Text="0" Foreground="{StaticResource TextLight}" FontSize="15" FontWeight="Bold" Margin="0,4,0,0"/>
                                            </StackPanel>
                                            <StackPanel Margin="0,0,10,12">
                                                <TextBlock Text="Vai trò / Vị trí:" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                                <TextBlock x:Name="TxtPlayerPosition" Text="Mid" Foreground="{StaticResource TextLight}" FontSize="15" FontWeight="Bold" Margin="0,4,0,0"/>
                                            </StackPanel>
                                            <StackPanel Margin="0,0,0,12">
                                                <TextBlock Text="Số trận thi đấu:" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                                <TextBlock x:Name="TxtPlayerMatchesPlayed" Text="0" Foreground="{StaticResource TextLight}" FontSize="15" FontWeight="Bold" Margin="0,4,0,0"/>
                                            </StackPanel>
                                            <StackPanel Margin="0,0,10,0">
                                                <TextBlock Text="Số lần đạt MVP:" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                                <TextBlock x:Name="TxtPlayerMvpCount" Text="0" Foreground="#10B981" FontSize="15" FontWeight="Bold" Margin="0,4,0,0"/>
                                            </StackPanel>
                                            <StackPanel>
                                                <!-- Empty cell for grid balance -->
                                            </StackPanel>
                                        </UniformGrid>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Right Panel: Recent Matches -->
                        <Border Grid.Column="1" Background="#0F172A" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="8" Padding="15">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Text="TRẬN ĐẤU GẦN ĐÂY" Foreground="{StaticResource TextMuted}" FontSize="11" FontWeight="Bold" Margin="0,0,0,10"/>
                                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                    <ItemsControl x:Name="ListPlayerRecentMatches">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,8" Cursor="Hand" MouseDown="MatchRow_MouseDown">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="80"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="80"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="{Binding Team1.Acronym}" Foreground="{StaticResource TextLight}" FontSize="11" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                                                            <TextBlock Text="{Binding Team1Score}" Foreground="{StaticResource TextLight}" FontSize="12" FontWeight="Black"/>
                                                            <TextBlock Text=":" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="3,0"/>
                                                            <TextBlock Text="{Binding Team2Score}" Foreground="{StaticResource TextLight}" FontSize="12" FontWeight="Black"/>
                                                        </StackPanel>
                                                        <TextBlock Grid.Column="2" Text="{Binding Team2.Acronym}" Foreground="{StaticResource TextLight}" FontSize="11" FontWeight="Bold" HorizontalAlignment="Right" TextTrimming="CharacterEllipsis"/>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
