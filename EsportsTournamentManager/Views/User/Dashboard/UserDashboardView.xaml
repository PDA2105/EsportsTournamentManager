<UserControl x:Class="EsportsTournamentManager.Views.User.Dashboard.UserDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="840">
    <UserControl.Resources>
        <!-- Hyperlink Button Style -->
        <Style x:Key="HyperlinkButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#6366F1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" TextDecorations="Underline" Foreground="{TemplateBinding Foreground}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#818CF8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Stats Grid Style (Read Only) -->
        <Style x:Key="StatDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="RowBackground" Value="#0F172A"/>
            <Setter Property="AlternatingRowBackground" Value="#1E293B"/>
            <Setter Property="Foreground" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="RowHeight" Value="32"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#1E293B"/>
                        <Setter Property="Foreground" Value="#9CA3AF"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="6,4"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#334155"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="6,0"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter SnapsToDevicePixels="True"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer x:Name="ScrollHomeDashboard" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,10,0">
                <!-- Quick Stats Grid -->
                <UniformGrid Columns="4" Margin="0,0,0,20">
                    <Border Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="GIẢI ĐẤU" Foreground="{StaticResource TextMuted}" FontSize="11" FontWeight="Bold"/>
                            <TextBlock x:Name="TxtTotalTournament" Text="0" Foreground="{StaticResource TextLight}" FontSize="24" FontWeight="Black" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,10,0" Cursor="Hand" MouseDown="BtnViewAllTeams_Click">
                        <StackPanel>
                            <TextBlock Text="ĐỘI TUYỂN" Foreground="{StaticResource TextMuted}" FontSize="11" FontWeight="Bold"/>
                            <TextBlock x:Name="TxtTotalTeam" Text="0" Foreground="{StaticResource TextLight}" FontSize="24" FontWeight="Black" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="TUYỂN THỦ" Foreground="{StaticResource TextMuted}" FontSize="11" FontWeight="Bold"/>
                            <TextBlock x:Name="TxtTotalPlayer" Text="0" Foreground="{StaticResource TextLight}" FontSize="24" FontWeight="Black" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Padding="15">
                        <StackPanel>
                            <TextBlock Text="TRẬN ĐẤU LIVE" Foreground="{StaticResource TextMuted}" FontSize="11" FontWeight="Bold"/>
                            <TextBlock x:Name="TxtActiveMatch" Text="0" Foreground="#10B981" FontSize="24" FontWeight="Black" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- Live Matches Section -->
                <TextBlock Text="🔴 TRẬN ĐẤU ĐANG DIỄN RA" Foreground="#EF4444" FontSize="13" FontWeight="Bold" Margin="0,0,0,10"/>
                <Border Background="#111827" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                    <Grid>
                        <ItemsControl x:Name="LiveMatchList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#1E1B4B" BorderBrush="#06B6D4" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,8" Cursor="Hand" MouseDown="LiveMatchRow_MouseDown">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                <Ellipse Width="8" Height="8" Fill="#6366F1" Margin="0,0,10,0"/>
                                                <TextBlock Text="{Binding Team1.TeamName}" Foreground="{StaticResource TextLight}" FontSize="13" FontWeight="Bold" MaxWidth="150" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Team1Score}" Foreground="{StaticResource TextLight}" FontSize="16" FontWeight="Black"/>
                                                <TextBlock Text=" - " Foreground="{StaticResource TextMuted}" FontSize="14" Margin="5,0"/>
                                                <TextBlock Text="{Binding Team2Score}" Foreground="{StaticResource TextLight}" FontSize="16" FontWeight="Black"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                                                <TextBlock Text="{Binding Team2.TeamName}" Foreground="{StaticResource TextLight}" FontSize="13" FontWeight="Bold" MaxWidth="150" TextTrimming="CharacterEllipsis"/>
                                                <Ellipse Width="8" Height="8" Fill="#EF4444" Margin="10,0,0,0"/>
                                            </StackPanel>
                                            <Border Grid.Column="3" Background="#EF4444" CornerRadius="4" Padding="6,2" Margin="20,0,0,0" VerticalAlignment="Center">
                                                <TextBlock Text="LIVE" Foreground="#FFFFFF" FontSize="10" FontWeight="Black"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock x:Name="TxtNoLiveMatches" Text="Hiện không có trận đấu nào đang trực tiếp" Foreground="{StaticResource TextMuted}" FontSize="13" FontStyle="Italic" HorizontalAlignment="Center" Margin="0,10,0,10" Visibility="Collapsed"/>
                    </Grid>
                </Border>

                <!-- Top Teams & Top Players Side-by-Side -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
 
                    <!-- Top Teams -->
                    <Border Grid.Column="0" Background="#111827" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="🔥 TOP ĐỘI TUYỂN" Foreground="{StaticResource TextLight}" FontSize="13" FontWeight="Bold" Margin="0,0,0,10" Cursor="Hand" MouseDown="BtnViewAllTeams_Click"/>
                            <DataGrid x:Name="GridTopTeams" Style="{StaticResource StatDataGrid}" Height="190" HeadersVisibility="Column" SelectionMode="Single">
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Header="Đội tuyển" Width="*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="{Binding TeamName}" Tag="{Binding Team}" Style="{StaticResource HyperlinkButton}" Click="TeamLink_Click" FontWeight="Bold"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="Thắng" Binding="{Binding Wins}" Width="50"/>
                                    <DataGridTextColumn Header="Trận" Binding="{Binding MatchesPlayed}" Width="50"/>
                                    <DataGridTextColumn Header="Tỉ lệ thắng" Binding="{Binding WinRateDisplay}" FontWeight="Bold" Width="80"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>

                    <!-- Top Players -->
                    <Border Grid.Column="1" Background="#111827" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="8" Padding="15" Margin="10,0,0,0">
                        <StackPanel>
                            <TextBlock Text="⭐ TOP TUYỂN THỦ (PTS TB)" Foreground="{StaticResource TextLight}" FontSize="13" FontWeight="Bold" Margin="0,0,0,10"/>
                            <DataGrid x:Name="GridTopPlayers" Style="{StaticResource StatDataGrid}" Height="190" HeadersVisibility="Column" SelectionMode="Single">
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Header="Tuyển thủ" Width="*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="{Binding InGameName}" Tag="{Binding Player}" Style="{StaticResource HyperlinkButton}" Click="PlayerLink_Click" FontWeight="Bold"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="Đội tuyển" Binding="{Binding TeamName}" Width="*"/>
                                    <DataGridTextColumn Header="Số trận" Binding="{Binding MatchesPlayed}" Width="60"/>
                                    <DataGridTextColumn Header="PTS TB" Binding="{Binding PointsDisplay}" FontWeight="Bold" Width="70"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- 5 Newest Tournaments Section -->
                <Grid Margin="0,10,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="🏆 GIẢI ĐẤU MỚI NHẤT" Foreground="{StaticResource TextLight}" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                </Grid>

                <Border Background="#111827" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                    <DataGrid x:Name="GridNewestTournaments" Style="{StaticResource StatDataGrid}" Height="190" HeadersVisibility="Column" SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Name" Width="3*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="{Binding Name}" Tag="{Binding TournamentId}" Style="{StaticResource HyperlinkButton}" Click="TournamentName_Click" FontWeight="Bold"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Trạng thái" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="4" Padding="6,2" HorizontalAlignment="Left" VerticalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                            <Setter Property="Background" Value="#374151"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Active">
                                                            <Setter Property="Background" Value="#1E1B4B"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                            <Setter Property="Background" Value="#064E3B"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding StatusDisplay}" Foreground="#FFFFFF" FontSize="11" FontWeight="Bold"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
