<Window x:Class="EsportsTournamentManager.Views.Admin.Teams.PlayerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EsportsTournamentManager.Views.Admin.Teams"
        mc:Ignorable="d"
        Title="Thông Tin Tuyển Thủ" Height="430" Width="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <!-- Custom Input Box Style -->
        <Style x:Key="DialogTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6366F1"/>
                                <Setter TargetName="border" Property="Background" Value="#0F172A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="12" Background="#0F172A" BorderBrush="#1E293B" BorderThickness="1">
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock x:Name="TxtTitle" Grid.Row="0" Text="Thêm Tuyển Thủ" Foreground="#F9FAFB" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- Inputs -->
            <StackPanel Grid.Row="1">
                <TextBlock Text="Tên Thi Đấu (In-Game Name) *" Foreground="#F9FAFB" FontSize="13" FontWeight="Medium" Margin="0,0,0,6"/>
                <TextBox x:Name="TxtInGameName" Style="{StaticResource DialogTextBox}" Margin="0,0,0,15"/>

                <TextBlock Text="Họ Tên Thật *" Foreground="#F9FAFB" FontSize="13" FontWeight="Medium" Margin="0,0,0,6"/>
                <TextBox x:Name="TxtRealName" Style="{StaticResource DialogTextBox}" Margin="0,0,0,15"/>

                <TextBlock Text="Vị Trí Thi Đấu" Foreground="#F9FAFB" FontSize="13" FontWeight="Medium" Margin="0,0,0,6"/>
                 <ComboBox x:Name="CboPosition" Style="{StaticResource PremiumComboBox}" Height="38" FontSize="14" Margin="0,0,0,15">
                    <ComboBoxItem Content="Top"/>
                    <ComboBoxItem Content="Jungle"/>
                    <ComboBoxItem Content="Mid"/>
                    <ComboBoxItem Content="Bot (ADC)"/>
                    <ComboBoxItem Content="Support"/>
                    <ComboBoxItem Content="Sub (Dự bị)"/>
                </ComboBox>
                <CheckBox x:Name="ChkIsActive" Content="Tuyển thủ đang hoạt động" Foreground="#F9FAFB" FontSize="14" IsChecked="True" VerticalContentAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="Hủy" Width="80" Height="35" Background="Transparent" Foreground="#9CA3AF" BorderBrush="#334155" Cursor="Hand" Click="CancelButton_Click" Margin="0,0,10,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="6">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1E293B"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Lưu" Width="80" Height="35" Background="#6366F1" Foreground="#FFFFFF" BorderBrush="Transparent" Cursor="Hand" Click="SaveButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#4F46E5"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
