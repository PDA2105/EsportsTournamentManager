<Window x:Class="EsportsTournamentManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EsportsTournamentManager"
        xmlns:localviews="clr-namespace:EsportsTournamentManager.Views"
        xmlns:admin="clr-namespace:EsportsTournamentManager.Views.Admin"
        xmlns:user="clr-namespace:EsportsTournamentManager.Views.User"
        xmlns:auth="clr-namespace:EsportsTournamentManager.Views.Auth"
        mc:Ignorable="d"
        Title="Esports Tournament Manager" 
        Height="650" Width="1100"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        MouseDown="Window_MouseDown">

    <Window.Resources />

    <Border CornerRadius="16" Background="#0F172A" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Effect="{StaticResource CardShadow}">
        <Grid>
            <!-- ========================================== -->
            <!-- 1. LOGIN CONTROL (Visible by Default)      -->
            <!-- ========================================== -->
            <auth:LoginView x:Name="LoginSection" Visibility="Visible"/>

            <!-- ========================================== -->
            <!-- 2. ADMIN APPLICATION WORKSPACE GRID        -->
            <!-- ========================================== -->
            <admin:AdminMain x:Name="AdminMainSection" Visibility="Collapsed"/>
            <user:UserMain x:Name="UserMainSection" Visibility="Collapsed"/>
        </Grid>
    </Border>
</Window>
